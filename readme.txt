v3

 fixed the following issues in the code:
    1. Reduce the text padding inside boxes to half of its current value on both sides. When writing text inside a box, pressing Shift+Enter should move to the next line. The default language should be English, with Chinese and Russian also added as options.
    2. Language settings should be changed via a dropdown menu.
    3. Change the tab/page title name from the current one to "Personal Mind Map".
    4. In desktop mode, the sidebar menu should be collapsible to free up more workspace area.
    5. When the page is refreshed, the project must not be lost. This is important — if the user tries to close the page, leave it, or refresh it without having clicked the save button first, a warning message must appear prompting them to save their file so that nothing is lost.
    6. In mobile mode, when the user pinch-zooms or edits a node and the keyboard appears, the top menu bar should remain fixed in place. Only the workspace area should be affected — the top menu should not move out of view or zoom. However, when zooming via the browser, the menus may scale.
    7. Add a button below the zoom controls that fits the entire chart/image to the screen — i.e., it should zoom in or out so that the whole project fits within the viewport.
    8. The color selection menu should be completely redesigned: the color circles should be separated from each other, and support for custom/user-defined colors should be added
    9. By default, any newly created node must inherit the color of its parent node.
    10. In desktop mode, when a node is selected and not in text-editing mode: the Delete key should delete the selected node; the Enter key should create a new child node beneath it; and the Tab key should insert a new node before it (as a previous sibling).
    11. When text inside a node is selected, the top toolbar should provide options for: left align, right align, center align, RTL & LTR text direction, numbered lists, bulleted lists, bold text, and font size adjustment.
    12. When the user clicks the Save button, a separate menu or submenu should appear, prompting the user to choose the save format. The user should be able to save as JSON, Markdown, JPG, PNG, SVG, and PDF.
    13. For JSON and Markdown files, the website address www.pmindmap.com must be included in the file header.
    14. For the other formats (images and PDF), the website address should be placed at the bottom of the output image or document.

v4

 The following features added:
	1. About Us Section in Sidebar
	2. Cryptocurrency Donation Section in Sidebar
	3. Feedback System in Sidebar
	4. Admin/Management Panel
v7
	very impruves!